1. Item struct (complete with operators + helpers) ✅ // structure that shows what term is to be executed and where the dot position is.

2. FIRST set ✅ // shows how each and every rule may start to help parser to reduce.
2.1 firstOfSequence() (CRITICAL) ✅ // tells the parser where to start.

3. Complete LR(1) Item struct (with operators + helpers) ✅ // Added operator overloading when using structs to help in later stages of the parser.

4. Closure() ✅

5. GOTO() ✅

6. Build Parser States (Canonical Collection) ✅

7. Build Parsing Tables (ACTION + GOTO) ✅

8. Detect conflicts ✅

9. Implement Shift-Reduce Parser ✅

10. Parse Tree (optional) ✅

11. Syntax Error Handling

12. Connect Lexer → Parser